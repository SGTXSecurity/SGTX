# IDOR 보안 취약점 시연 가이드 (학생 프로젝트용)

이 문서는 과도한 권한 부여(IDOR, Insecure Direct Object Reference) 취약점을 영상으로 시연하기 위한 가이드입니다. 복잡한 서버 구축 없이 프론트엔드 단일 파일로 동작합니다.

## 1. 파일 확인
프로젝트 내 `src/main/resources/templates/idor-student.html` 파일이 준비되어 있습니다. 
이 파일은 대학생 수준의 단순한 UI(바닐라 JS, 기본 HTML)로 구현되어 있어, "학습을 위해 급하게 짠 테스트 페이지"라는 느낌을 주기 좋습니다.

## 2. 실행 방법
별도의 서버(Spring Boot)를 띄울 필요가 없습니다.
해당 `idor-student.html` 파일을 크롬 등 웹 브라우저로 직접 끄집어다 놓거나(Drag & Drop) 더블클릭하여 엽니다.

## 3. 영상 촬영 (시연) 시나리오

영상을 찍을 때 다음 순서대로 진행하면 "어떻게 타인의 정보를 빼내는지"를 명확하게 보여줄 수 있습니다.

### 단계 1: 일반적인 정상 사용 보여주기
1. **[1. 현재 세션 설정]**에서 `User 1 (일반 사용자)`를 선택합니다.
2. **[2. API 요청]**의 영수증 번호 칸에 `1`을 입력하고 [조회하기] 버튼을 누릅니다.
3. 정상적으로 User 1의 결제 정보(비자카드, 금액 등)가 조회되는 것을 보여줍니다.
4. *설명:* "일반적인 사용자는 자신의 결제 내역인 1번을 정상적으로 조회할 수 있습니다."

### 단계 2: 취약점(IDOR) 공격 시연
1. **[1. 현재 세션 설정]**에서 `User 999 (해커/악의적 사용자)`로 변경합니다. (이것은 해커가 자신의 계정으로 로그인했음을 의미합니다.)
2. **[2. API 요청]**의 영수증 번호 칸의 숫자를 `1`로 그대로 둡니다. (또는 2로 변경해 봅니다.)
3. [조회하기] 버튼을 누릅니다.
4. *결과:* 해커(User 999)의 계정임에도 불구하고, User 1의 민감한 카드 정보가 화면과 "API 응답 Raw Data" 영역에 그대로 출력됩니다.
5. *설명:* "하지만 서버(백엔드)에서 요청자가 해당 데이터의 진짜 주인인지 확인하지 않기 때문에, 악의적인 사용자가 숫자만 바꾸면 다른 사람의 카드 정보를 쉽게 탈취할 수 있습니다. 이것이 IDOR 취약점입니다."

## 4. 백엔드(서버) 코드 개선 설명하기
영상 말미에, 소스코드의 어떤 부분을 고쳐야 이 문제가 해결되는지 설명하면 좋은 평가를 받을 수 있습니다.

`idor-student.html` 파일 내부(약 65번 라인 부근)에 주석 처리된 부분을 보여주며 설명합니다.

```javascript
// [핵심 취약점] 권한 검증 로직 누락!
// 정상적인 코드라면 아래 주석처럼 소유자를 확인해야 합니다.
/* 
if (data.ownerId !== currentSessionUser) {
    showError("권한이 없습니다. (403 Forbidden)", data);
    return;
}
*/
```

"백엔드의 Controller 나 Service 단에서 `현재 로그인한 사용자의 ID`와 `조회하려는 데이터의 Owner ID`가 일치하는지 비교하는 단 한 줄의 로직만 추가해도 이 치명적인 취약점을 막을 수 있습니다." 라고 결론을 냅니다.